{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Client",
            "type": "node-terminal",
            "request": "launch",
            "cwd": "${workspaceFolder}",
            "command": "${workspaceFolder}\\gradlew.bat runClient"
        }
    ]
}